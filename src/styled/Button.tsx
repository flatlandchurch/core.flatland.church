import styled from 'styled-components';

const Button = styled.button`
  background: #a8458c;
  color: #fff;
  border-radius: 50px;
  padding: 12px 68px;
  margin: 24px auto;
  display: block;
  appearance: none;
  border: 0;
  font-size: inherit;
  font-weight: 600;
  cursor: pointer;
`;

export default Button;
